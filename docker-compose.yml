services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: hive_delivery
      POSTGRES_USER: hive
      POSTGRES_PASSWORD: hive
    ports: ["5432:5432"]
    volumes: ["hive_delivery_pg:/var/lib/postgresql/data"]
volumes: { hive_delivery_pg: {} }
